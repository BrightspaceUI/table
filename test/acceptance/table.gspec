@set
	path d2l-table

@import table-common.gspec

== D2L Table LTR ==

	wide.right-action:
		inside partly wide 10px top, -15px right
		image file dumps/${path}/objects/wide.right-action.png, error 14px
