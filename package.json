{
  "name": "d2l-table",
  "description": "Mixins and CSS for applying table styles",
  "private": true,
  "scripts": {
    "clean": "rimraf test/acceptance/dumps/* reports/",
    "postinstall": "bower install",
    "predump": "npm run -s clean",
    "dump": "npm run -s dump:galen:local",
    "test": "concurrently -p name -n lint:js,lint:wc,galen,wct \"npm run test:lint:js\" \"npm run test:lint:wc\" \"npm run test:galen:local\" \"wct --skip-plugin sauce\"",
    "test:lint:js": "eslint .",
    "test:lint:wc": "polylint --no-recursion --input *.html",
    "dump:galen:local:run": "galen test test/acceptance/dump.test",
    "dump:galen:local": "concurrently -p name -n serve,galen -s first -k \"polymer serve\" \"npm run dump:galen:local:run\"",
    "galen:local:run": "galen test test/acceptance/check-local.test --htmlreport reports/d2l-table",
    "galen:sauce:run": "galen test test/acceptance/check-sauce.test --htmlreport reports/d2l-table -p4 -DUSERNAME=$SAUCE_USERNAME -DACCESS_KEY=$SAUCE_ACCESS_KEY -DTUNNEL_IDENTIFIER=$TRAVIS_JOB_NUMBER",
    "test:galen:local": "concurrently -p name -n serve,galen -s first -k \"polymer serve\" \"npm run galen:local:run\"",
    "test:galen:sauce": "concurrently -p name -n serve,galen -s first -k \"polymer serve\" \"npm run galen:sauce:run\""
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Brightspace/d2l-table-ui.git"
  },
  "keywords": [
    "d2l",
    "polymer",
    "table"
  ],
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "dependencies": {
    "bower": "^1.7.7"
  },
  "devDependencies": {
    "concurrently": "^2.2.0",
    "eslint": "^3.3.1",
    "eslint-config-brightspace": "^0.2.1",
    "eslint-plugin-html": "^1.5.2",
    "galenframework": "^2.3.0",
    "phantomjs-prebuilt": "^2.1.4",
    "polylint": "^2.10.1",
    "polymer-cli": "^0.16.0",
    "rimraf": "^2.5.2",
    "static-server": "^2.0.3",
    "web-component-tester": "^4.3.4"
  }
}
