@objects

	table-*       table
		head      thead
		foot      tfoot
		body      tbody
		tr-*      tr

== D2L Table ==

	@forEach [table-*] as table
		@forEach [${table}.tr-*] as tr
			${tr}:
				image file dumps/vui-table/objects/${tr}.png
